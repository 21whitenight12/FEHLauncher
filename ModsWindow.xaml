<Window x:Class="FalloutLauncher.ModsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление модами"
        Height="520" Width="520"
        MinHeight="350" MinWidth="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Background="#FF0D1117"
        Closing="Window_Closing">

    <Window.Resources>
        <Color x:Key="AccentColor">#FF7C4DFF</Color>
        <SolidColorBrush x:Key="BgBrush" Color="#FF0D1117"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#FF161B22"/>
        <SolidColorBrush x:Key="CardBrush" Color="#FF21262D"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#FF30363D"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#FFE6EDF3"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#FF8B949E"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#FF7C4DFF"/>

        <FontFamily x:Key="FontPrimary">Segoe UI Variable, Segoe UI, sans-serif</FontFamily>
        <FontFamily x:Key="FontMono">Cascadia Code, Consolas, monospace</FontFamily>

        <Style x:Key="ModBtnStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource CardBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="FontFamily" Value="{StaticResource FontPrimary}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="16,6"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="root" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="root" Property="Background">
                                    <Setter.Value>
                                        <SolidColorBrush Color="#FF7C4DFF" Opacity="0.2"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SeparatorStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="FontFamily" Value="{StaticResource FontPrimary}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontStyle" Value="Italic"/>
            <Setter Property="Margin" Value="4,8,0,4"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Style="{DynamicResource CardStyle}"
                Padding="14,10"
                Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Path Grid.Column="0"
                      Width="18" Height="18" Stretch="Uniform"
                      Stroke="{DynamicResource TextPrimaryBrush}" 
                      StrokeThickness="1.8" Fill="Transparent"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      Margin="0,0,10,0"
                      Data="M21,16 V8 A2,2 0 0,0 20,6.27 L13,2.27 A2,2 0 0,0 11,2.27 L4,6.27 A2,2 0 0,0 3,8 V16 A2,2 0 0,0 4,17.73 L11,21.73 A2,2 0 0,0 13,21.73 L20,17.73 A2,2 0 0,0 21,16 Z M3.27,6.96 L12,12.01 L20.73,6.96 M12,22.08 L12,12"/>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="Управление модами"
                               FontFamily="{StaticResource FontPrimary}"
                               FontSize="15"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                    <TextBlock Text="Включайте и отключайте моды для профиля ChikadoZ"
                               FontFamily="{StaticResource FontPrimary}"
                               FontSize="11"
                               Foreground="{DynamicResource TextSecondaryBrush}"/>
                </StackPanel>


            </Grid>
        </Border>

        <!-- Mod list -->
        <Border Grid.Row="1"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="4">
            <ScrollViewer Style="{DynamicResource ModernScrollViewer}">
                <ItemsControl x:Name="itemsControl" Margin="6,4"/>
            </ScrollViewer>
        </Border>

        <!-- Footer buttons -->
        <Border Grid.Row="2"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="14,8"
                Margin="0,12,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnSave"
                        Content="✅ Сохранить"
                        Style="{StaticResource ModBtnStyle}"
                        Click="BtnSave_Click"
                        Margin="0,0,8,0" Width="90"/>
                <Button x:Name="btnCancel"
                        Content="❌ Отмена"
                        Style="{StaticResource ModBtnStyle}"
                        Click="BtnCancel_Click" Width="75"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>